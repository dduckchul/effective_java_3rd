    
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/**" /> 
            <bean class="ssg.ad.cm.interceptor.UserAuthCheckInterceptor" />
        </mvc:interceptor>
    </mvc:interceptors>

